****
The following commands to be written to compile, insert and clean the kernel code

To compile, type 
        make all

To clean , type 
        make clean

To insert module into kernel
        sudo insmod usb_to_i2c.ko

To remove modules from kernel
        sudo rmmod usb_to_i2c

